// Remote MCP transport with OAuth — paid surface
// TODO: Implement when ready to monetize
export async function startRemoteServer(port: number = 3001) {
  console.log(`Remote MCP server placeholder — port ${port}`);
  console.log("This will be the hosted OAuth endpoint for teams/scale.");
}
